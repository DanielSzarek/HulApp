<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">
    <Transition
        android:id="@+id/loginTransition"
        motion:constraintSetStart="@layout/fragment_login"
        motion:constraintSetEnd="@id/endConstraintSet"
        motion:duration="@integer/default_motion_scene_transition_duration" />

    <ConstraintSet
        android:id="@+id/endConstraintSet">

        <Constraint
            android:id="@id/progressBar"
            motion:layout_constraintStart_toStartOf="parent"
            motion:layout_constraintEnd_toEndOf="parent"
            motion:layout_constraintTop_toTopOf="@id/loginButton"
            motion:layout_constraintBottom_toBottomOf="@id/loginButton" />

        <Constraint
            android:id="@id/loginButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            motion:layout_constraintEnd_toStartOf="parent"
            motion:layout_constraintTop_toBottomOf="@id/passwordResetButton" />
    </ConstraintSet>
</MotionScene>